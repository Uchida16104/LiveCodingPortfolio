setResolution(1600,1600);
osc(1, 2, 300)
	.diff(gradient(1))
	.diff(solid([0, 1].smooth()
		.fast(1 / 8), [0, 1].smooth()
			.fast(1 / 4), [0, 1].smooth()
			.fast(1 / 2), [0, 1].smooth()))
.hue()
.saturate()
.brightness()
.shift()
.contrast()
.colorama()
.modulate(voronoi())
	.out()
screencap()